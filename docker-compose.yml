services:
  telvyn-ai:
    build: .
    container_name: telvyn-ai-agent
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./knowledge:/app/knowledge
    env_file:
      - .env
    restart: unless-stopped
